# Legacy SQL Migrations (Deprecated)

Do not use these SQL files for new environments.

Source of truth is Alembic migrations in `packages/db/src/db/migrations`.

This folder exists only for historical reference and emergency rollback debugging.
